# קאסה — הנחיות לעבודה בריפו

מערכת ניהול בית. הקוד באנגלית, כל טקסט שמשתמש רואה — בעברית.

לפני שינוי משמעותי קראו את [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md); הוא מסביר
*למה* דברים בנויים כפי שהם, וכמה מההחלטות שם הן הגנות שקל לבטל בטעות.

## הכללים שלא נשברים

- **כסף הוא `NUMERIC(12,2)`.** אף פעם לא `REAL` / `FLOAT`. בדיקה אוכפת את זה.
- **`amount < 0` = הוצאה, `amount > 0` = הכנסה.** אין עמודת `type`, ולא תהיה.
- **מחיקה רכה** בכל מקום שיש בו כסף או היסטוריה (`deleted_at` / `archived_at`).
- **כל שינוי סכימה הוא `IF NOT EXISTS`.** המיגרציה היא כפתור שנלחץ שוב ושוב.
- אחרי עריכת `db/schema.sql` הריצו **`npm run schema:build`** ו-commit לשני הקבצים.
- **לוגיקה טהורה הולכת ל-`shared/`** ומקבלת בדיקה. `api/` שואל את המסד; `src/` מרנדר.
- **תפקיד המשתמש נקרא מהמסד בכל בקשה**, אף פעם לא מהטוקן.
- מגבלת Vercel: **12 פונקציות**. אנחנו על 6. endpoint חדש נכנס לראוטר קיים,
  לא לקובץ חדש תחת `api/`.

## לפני commit

```bash
npm run check    # schema drift + typecheck + tests + build
```

## סגנון

- הערות מסבירות **למה**, לא מה. אם הקוד ברור, אין צורך בהערה.
- טקסט משתמש בעברית, ישיר, בלי סימני קריאה.
- אין ספריות חדשות בלי סיבה טובה — כרגע: react, pg, google-auth-library, web-push.
